import styled from "styled-components";
import {
  FormWrapper as FormWrapperAddRecipient,
  Input as InputAddRecipient,
  Span as SpanAddRecipient,
  Button as ButtonAddRecipient,
} from "pages/AddRecipient/AddRecipient.css";

export const Wrapper = styled.div``;

export const FormWrapper = styled(FormWrapperAddRecipient)``;

export const Input = styled(InputAddRecipient)``;
export const Span = styled(SpanAddRecipient)``;
export const Button = styled(ButtonAddRecipient)``;
